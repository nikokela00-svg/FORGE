# Boot Feature

A temporary proof-of-life vertical slice: the typographic wordmark screen
shown before the real workspace lands. It exists to demonstrate the
feature-slice layout (ui/ + barrel `index.ts`) the constitution mandates.

This slice will be replaced in the next phase; nothing downstream should
depend on its internals — import only from the barrel `./index.ts`.
