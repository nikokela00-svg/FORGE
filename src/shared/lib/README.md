# shared/lib

Pure, dependency-light utilities shared across features.

- No React imports. Node-free. Browser-safe. Zod-safe at every boundary.
- Every module ships with unit tests in `tests/unit/shared/lib/`.
- Keep functions pure and under 40 lines (constitution).
