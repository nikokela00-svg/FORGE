# FORGE environment variables

# No runtime environment variables are required in v1 — the entire application
# runs client-side with no server. The variables below are reserved for the
# AI provider integrations landing in a later phase.

# AI provider API key (reserved — never committed, never bundled)
# FORGE_AI_API_KEY=