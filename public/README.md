# public

Static assets served at the site root.

Rules:
- Small, hand-curated assets only (favicons, manifests, robots). No
  generated bundles, no image blobs.
- Test fixtures and runtime data belong elsewhere.
- Textual assets are prettier-formatted and reviewed like code.